# OCR mock

Clone the repo, then:

`
python -m venv  ./venv
`

Activate venv...
... in UNIX:
`
. ./venv/bin/activate
`
... in Windows:
`
./venv/Scripts/Activate.bat
`

Then install deps:
`
pip install -r ./requirements.txt
`

To run (will consume **one** image, exactly):

`
python main.py
`